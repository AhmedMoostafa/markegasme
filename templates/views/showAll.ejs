<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <title>Document</title>
</head>
<body>
    <% posts.forEach(element => { %>
        <div class="card mt-4">
          <div class="card-body">
            <h1 class="card-title" style="font-weight: bold;"><%= element.title %></h1>
            <div class="card-subtitle text-muted mb-2">
              <%= element.createdAt.toLocaleDateString() %>
            </div>
            <div class="card-text mb-2"><h2><%= element.description %></h2></div>
            <div class="card-text mb-2"><%= element.post %></div>4
            <% if (admin!=undefined) { %>
              <a href="/edit/<%= element.id %>" class="btn btn-info">Edit</a>
              <form action="/delete/<%= element.id %>" method="POST" class="d-inline">
                  <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            <% } %>
           
           
          </div>
       
        </div>
      <% }) %>
      <a href="/" class="btn btn-secondary">back</a>
</body>
</html>